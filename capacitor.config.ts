import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.monsterdroid',
  appName: 'FlashLight',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
